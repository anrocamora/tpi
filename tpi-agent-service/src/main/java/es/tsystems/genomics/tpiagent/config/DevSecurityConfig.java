package es.tsystems.genomics.tpiagent.config;

/**
 * Seguridad desactivada temporalmente.
 *
 * Este archivo se mantiene para registrar la decisión y evitar imports/clases perdidas en el historial.
 */
public final class DevSecurityConfig {
    private DevSecurityConfig() {
    }
}
